///
/// Fit.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::rive {

  /**
   * An enum which can be represented as a JavaScript enum (Fit).
   */
  enum class Fit {
    FILL      SWIFT_NAME(fill) = 0,
    CONTAIN      SWIFT_NAME(contain) = 1,
    COVER      SWIFT_NAME(cover) = 2,
    FITWIDTH      SWIFT_NAME(fitwidth) = 3,
    FITHEIGHT      SWIFT_NAME(fitheight) = 4,
    NONE      SWIFT_NAME(none) = 5,
    SCALEDOWN      SWIFT_NAME(scaledown) = 6,
    LAYOUT      SWIFT_NAME(layout) = 7,
  } CLOSED_ENUM;

} // namespace margelo::nitro::rive

namespace margelo::nitro {

  // C++ Fit <> JS Fit (enum)
  template <>
  struct JSIConverter<margelo::nitro::rive::Fit> final {
    static inline margelo::nitro::rive::Fit fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      int enumValue = JSIConverter<int>::fromJSI(runtime, arg);
      return static_cast<margelo::nitro::rive::Fit>(enumValue);
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::rive::Fit arg) {
      int enumValue = static_cast<int>(arg);
      return JSIConverter<int>::toJSI(runtime, enumValue);
    }
    static inline bool canConvert(jsi::Runtime&, const jsi::Value& value) {
      if (!value.isNumber()) {
        return false;
      }
      double number = value.getNumber();
      int integer = static_cast<int>(number);
      if (number != integer) {
        // The integer is not the same value as the double - we truncated floating points.
        // Enums are all integers, so the input floating point number is obviously invalid.
        return false;
      }
      // Check if we are within the bounds of the enum.
      return integer >= 0 && integer <= 7;
    }
  };

} // namespace margelo::nitro
