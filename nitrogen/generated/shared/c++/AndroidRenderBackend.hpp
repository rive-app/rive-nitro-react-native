///
/// AndroidRenderBackend.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::rive {

  /**
   * An enum which can be represented as a JavaScript union (AndroidRenderBackend).
   */
  enum class AndroidRenderBackend {
    OPENGL      SWIFT_NAME(opengl) = 0,
    VULKAN      SWIFT_NAME(vulkan) = 1,
  } CLOSED_ENUM;

} // namespace margelo::nitro::rive

namespace margelo::nitro {

  // C++ AndroidRenderBackend <> JS AndroidRenderBackend (union)
  template <>
  struct JSIConverter<margelo::nitro::rive::AndroidRenderBackend> final {
    static inline margelo::nitro::rive::AndroidRenderBackend fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("opengl"): return margelo::nitro::rive::AndroidRenderBackend::OPENGL;
        case hashString("vulkan"): return margelo::nitro::rive::AndroidRenderBackend::VULKAN;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum AndroidRenderBackend - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::rive::AndroidRenderBackend arg) {
      switch (arg) {
        case margelo::nitro::rive::AndroidRenderBackend::OPENGL: return JSIConverter<std::string>::toJSI(runtime, "opengl");
        case margelo::nitro::rive::AndroidRenderBackend::VULKAN: return JSIConverter<std::string>::toJSI(runtime, "vulkan");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert AndroidRenderBackend to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("opengl"):
        case hashString("vulkan"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
