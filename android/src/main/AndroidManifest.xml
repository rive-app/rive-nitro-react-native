<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application>
    <provider
      android:name=".CoverageInitProvider"
      android:authorities="${applicationId}.rive.coverage"
      android:exported="false" />
  </application>
</manifest>
